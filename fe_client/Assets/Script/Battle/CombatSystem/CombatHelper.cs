using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Battle
{
    // public static class CombatHelper
    // {
    //     //TODO: 전투 결과/예측값을  List<CombatStep> 형태로 반환.
    //     public static void Setup(bool _is_plan, Int64 _attacker_id, Int64 _target_id, Int64 _weapon_id)
    //     {
            

    //         Entity attacker = EntityManager.Instance.GetEntity(_attacker_id);
    //         Entity target   = EntityManager.Instance.GetEntity(_target_id);   

    //         CombatSystemManager.Instance.Setup(CombatParam_Plan.Cached.Set(attacker, target));

    //         while (CombatSystemManager.Instance.IsFinished == false)
    //         {
    //             // 
    //             CombatSystemManager.Instance.Update();

                
    //         }

            
    //     }
    // }
}